{{ attach_library('aries/molecule--menu-item--mega-menu-root-item') }}
{{ link|with(['#attributes', 'class'], 'molecule--menu-item--mega-menu-root-item') }}

{#

  need to figure out how to render the content from the two blocks to resemble
  the menu structure to let the drilldown and dropdown js act on it properly.

#}
{{ content }}
