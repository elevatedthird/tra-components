args:
  menu_level_template_map:
    required: false
    type: array
    default_value:
    example: {
      default: '@component/menu-item/top-bar-link/top-bar-link.twig',
      1: '@component/menu-item/top-bar-link/top-bar-link--1.twig',
    }
    notes: a array where the key is the specific menu level. Default is used if level not specified.

